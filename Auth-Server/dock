version: '3'
services:
  mongodb:
    image: mongo
    container_name: mongodb_users
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: nome_utente
      MONGO_INITDB_ROOT_PASSWORD: password
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
